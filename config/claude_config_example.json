{
  "mcpServers": {
    "telegram": {
      "command": "npx",
      "args": ["-y", "@chaindead/telegram-mcp"],
      "env": {
        "TG_APP_ID": "${TG_APP_ID}",
        "TG_API_HASH": "${TG_API_HASH}"
      }
    }
  }
}